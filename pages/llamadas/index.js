import React from "react";
import { Container } from '@mui/material'
import Calls from '../../components/llamadas'

const Llamadas = () => {
  return (
    <Container maxWidth="lg">
      <Calls />
    </Container>
  );
};

export default Llamadas;